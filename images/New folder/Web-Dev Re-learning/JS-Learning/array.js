var num = [12, 23, 34, 45, 32];

// changing element by index number
num[0] = 20;

//taking number from an array
var element = num[2];
console.log(element);
console.log(num);

//printing the length of the array
console.log(num.length);

//finding the number of array index
//var positionIndex = num.indexOf(45);
console.log(num.indexOf(45));
//finding the number of array index which doesn't exsist in the array
console.log(num.indexOf(190));